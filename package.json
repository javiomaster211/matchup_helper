{
  "name": "matchuphelper",
  "version": "1.0.0",
  "description": "Lightweight desktop app for managing LoL matchup notes",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "serve": "npx serve src -l 1420"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "serve": "^14.2.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
